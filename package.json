{
  "name": "react-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@okta/okta-react": "^1.2.0",
    "@okta/okta-signin-widget": "^2.15.0",
    "dotenv": "^6.2.0",
    "fs": "0.0.1-security",
    "noty": "^3.2.0-beta",
    "react": "^16.8.4",
    "react-bootstrap": "^1.0.0-beta.5",
    "react-dom": "^16.8.4",
    "react-router-dom": "^4.3.1",
    "react-scripts": "^2.1.3",
    "redux": "^4.0.1",
    "replace-in-file": "^3.4.4"
  },
  "scripts": {
    "make": "node make.js",
    "start": "npm run make && react-scripts start",
    "build": "npm run make && react-scripts build",
    "test": "npm run make && react-scripts test",
    "eject": "npm run make && react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "terser": "^3.17.0"
  }
}
